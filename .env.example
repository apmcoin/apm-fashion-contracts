# Local secrets - gitignored, NEVER commit.
# Paste the values you generated; leave unused ones blank.

# Throwaway deployer (gas only; ownerless token = no lasting power).
DEPLOYER_PRIVATE_KEY=

# RPC endpoints. Use a controlled endpoint for production deployment.
BSC_RPC=
BSC_TESTNET_RPC=
